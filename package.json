{
  "name": "arash-terminal",
  "version": "1.0.0",
  "description": "Web-based terminal accessible from your phone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "node server.js --tunnel",
    "gen-cert": "node -e \"require('fs').mkdirSync('certs',{recursive:true})\" && openssl req -x509 -newkey rsa:2048 -keyout certs/key.pem -out certs/cert.pem -days 365 -nodes -subj \"/CN=arash-terminal\""
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "localtunnel": "^2.0.2",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
